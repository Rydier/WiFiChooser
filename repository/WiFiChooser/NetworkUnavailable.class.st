"
NetworkUnavailable is an error raised if connection to a network fails due to not being able to find a network with the specified SSID
"
Class {
	#name : #NetworkUnavailable,
	#superclass : #WiFiConnectionError,
	#category : #WiFiChooser
}
