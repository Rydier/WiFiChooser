"
IncorrectCredentials is an error raised if connection to a network fails because invalid credentials are being used.
"
Class {
	#name : #IncorrectCredentials,
	#superclass : #WiFiConnectionError,
	#category : #WiFiChooser
}
